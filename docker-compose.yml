version: '3.8'
services:
  api:
    container_name: ms-vecinet-post
    build: .
    ports:
      - "8082:8082"
    env_file:
      - .env
    volumes:
      - .:/app
      - gradle-cache:/home/gradle/.gradle
volumes:
  gradle-cache: